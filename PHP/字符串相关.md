# 字符处理相关

```
strpos() - 查找字符串首次出现的位置      stripos（不区分大小写）
strrpos  -  查找字符串最后出现的位置  strripos()（不区分大小写）
strrchr() - 查找指定字符在字符串中的最后一次出现
strstr() - 查找字符串的首次出现   stristr() （不区分大小写）

substr
str_replace

strlen
substr_count — 计算字串出现的次数
string strrev — 反转字符串
str_repeat重复字符串
str_split分割成数组

stripslashes — 反引用一个引用字符串
stripcslashes — 反引用一个使用 转义的字符串
addcslashes
```